services:
  db:
    image: 'postgres:16-alpine'
    container_name: vantryx-db
    environment:
      - 'POSTGRES_DB=vantryx_db'
      - 'POSTGRES_PASSWORD=admin'
      - 'POSTGRES_USER=admin'
    ports:
      - '5432:5432'